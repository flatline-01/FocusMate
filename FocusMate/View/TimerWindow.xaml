<Window x:Class="FocusMate.TimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusMate"
        mc:Ignorable="d"
        Title="Timer" Height="400" Width="400" ResizeMode="CanMinimize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="#952C2C">
        <Grid.RowDefinitions>
            <RowDefinition Height="236*"/>
            <RowDefinition Height="71*"/>
            <RowDefinition Height="77*"/>
        </Grid.RowDefinitions>
        
        
        <Image Source="/Resource/pomodoro_icon.png" Width="250" Panel.ZIndex="-1" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0"/>
        <TextBlock x:Name="TimerDisplay" FontSize="80"  Width="300" Text="00:00" TextAlignment="Center" FontWeight="Bold" Foreground="#ffff" 
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="0">
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="4" Direction="330" Color="Black" Opacity="0.5" BlurRadius="4"/>
            </TextBlock.Effect>
        </TextBlock>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Style="{StaticResource CustomLabel}" Content="Task:" Grid.Row="1" FontSize="24" Width="70"/>
            <TextBlock x:Name="TaskTitle" TextWrapping="Wrap" Width="200" FontSize="16" Foreground="#ffff" VerticalAlignment="Center"/>
        </WrapPanel>
        
        <WrapPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Stop" Click="StopTimerButtonClick" Style="{StaticResource BtnRed}" Width="120"/>
            <Button Content="Continue" Click="ContinueTimerButtonClick" Margin="50,0,0,0" Style="{StaticResource BtnGreen}" Width="120"/>
        </WrapPanel>
    </Grid>
</Window>