<Window x:Class="FocusMate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusMate"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="45*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="55*"/>
        </Grid.RowDefinitions>
        
        <DataGrid x:Name="TasksList" d:ItemsSource="{d:SampleData ItemCount=10}" Grid.Row="1" Grid.Column="1"/>
        <Button Grid.Column="1" Content="Add Task" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Center" Width="64"/>

    </Grid>
</Window>
